services:
  portfolio:
    build:
      context: .
      dockerfile: dockerfile
      args:
        VITE_WEB3FORMS_KEY: ${VITE_WEB3FORMS_KEY}
    image: personal-portfolio-website:latest
    container_name: personal-portfolio-website-container
    ports:
      - "5173:80"
    restart: unless-stopped
    environment:
      - VITE_WEB3FORMS_KEY=${VITE_WEB3FORMS_KEY}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
